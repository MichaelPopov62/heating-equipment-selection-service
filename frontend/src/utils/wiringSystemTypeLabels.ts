/**
 * Назначение: подписи типов разводки радиаторов для UI анкеты.
 */

import type { WiringSystemType } from '../surveySession/wiringLayoutV3';

/** Опция radio-группы «тип разводки» на шаге «Гидравлика». */
export type WiringSystemTypeOption = {
  value: WiringSystemType;
  /** Заголовок пункта в radio-списке. */
  label: string;
  /** Пояснение под заголовком. */
  description: string;
  /** Бейдж «Рекомендуется» (только auto). */
  recommended?: boolean;
};

/** Вертикальный список схем разводки (порядок = порядок в UI). */
export const WIRING_SYSTEM_TYPE_OPTIONS: ReadonlyArray<WiringSystemTypeOption> = [
  {
    value: 'auto',
    label: 'Авто (оптимальный подбор системы)',
    description:
      'Система сама сгруппирует радиаторы: большие комнаты получат отдельные ветки, '
      + 'а мелкие зоны объединятся в микроколлекторы для экономии труб.',
    recommended: true,
  },
  {
    value: 'two-pipe-dead-end',
    label: 'Двухтрубная с «тупиками»',
    description:
      'Последовательная магистраль через комнаты. Диаметр труб будет автоматически '
      + 'уменьшаться к концу ветки.',
  },
  {
    value: 'two-pipe-pass',
    label: 'Двухтрубная проходная (петля Тихельмана)',
    description:
      'Магистраль с постоянным расходом по всей длине; типичная проходная схема. '
      + 'Диаметр trunk не уменьшается по ходу, в отличие от тупиковой.',
  },
  {
    value: 'manifold',
    label: 'Коллекторная лучевая (выделенная)',
    description:
      'Бескомпромиссная параллельная схема: отдельная пара труб к каждому радиатору '
      + 'от распределительного узла, без объединения мелких зон.',
  },
];
