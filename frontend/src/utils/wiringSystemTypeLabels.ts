/**
 * Назначение: подписи типов разводки радиаторов для UI анкеты.
 */

import type { WiringSystemType } from '../surveySession/wiringLayoutV3';

/** Опция radio-группы «тип разводки» на шаге «Гидравлика». */
export type WiringSystemTypeOption = {
  value: WiringSystemType;
  /** Заголовок пункта в radio-списке. */
  label: string;
  /** Пояснение под заголовком. */
  description: string;
  /** Бейдж «Рекомендуется» (только auto). */
  recommended?: boolean;
};

/** Вертикальный список схем разводки (порядок = порядок в UI). */
export const WIRING_SYSTEM_TYPE_OPTIONS: ReadonlyArray<WiringSystemTypeOption> = [
  {
    value: 'auto',
    label: 'Авто (оптимальний підбір системи)',
    description:
      'Система сама згрупує радіатори: великі кімнати отримають окремі гілки, '
      + 'а дрібні зони об\'єднаються в мікроколектори для економії труб.',
    recommended: true,
  },
  {
    value: 'two-pipe-dead-end',
    label: 'Двотрубна з «тупиками»',
    description:
      'Послідовна магістраль через кімнати. Діаметр труб буде автоматично '
      + 'зменшуватися до кінця гілки.',
  },
  {
    value: 'two-pipe-pass',
    label: 'Двотрубна прохідна (петля Тіхельмана)',
    description:
      'Магістраль з постійною витратою по всій довжині; типова прохідна схема. '
      + 'Діаметр trunk не зменшується по ходу, на відміну від тупикової.',
  },
  {
    value: 'manifold',
    label: 'Колекторна променева (виділена)',
    description:
      'Безкомпромісна паралельна схема: окрема пара труб до кожного радіатора '
      + 'від розподільного вузла, без об\'єднання дрібних зон.',
  },
];
