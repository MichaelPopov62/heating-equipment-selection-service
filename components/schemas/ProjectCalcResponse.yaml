type: object
required: [ok, report, calculation, project]
properties:
  ok: { type: boolean, enum: [true] }
  report:
    $ref: CalcReport.yaml
  calculation:
    $ref: CalculationListItem.yaml
  project:
    $ref: ProjectDetail.yaml
