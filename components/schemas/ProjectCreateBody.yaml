type: object
additionalProperties: false
required: [clientName]
properties:
  clientName:
    type: string
    minLength: 1
    maxLength: 200
  label:
    type: string
    maxLength: 200
  survey:
    type: object
    description: >
      Черновик анкеты с фронта (schemaVersion, rooms, objectMeta, …).
      Лимит размера JSON — 512 KB (413 PAYLOAD_TOO_LARGE).
