type: object
additionalProperties: false
minProperties: 1
properties:
  clientName:
    type: string
    minLength: 1
    maxLength: 200
  label:
    type: string
    maxLength: 200
    nullable: true
  survey:
    type: object
    description: >
      Черновик анкеты с фронта. Лимит размера JSON — 512 KB (413 PAYLOAD_TOO_LARGE).
