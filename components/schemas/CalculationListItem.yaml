type: object
required: [id, projectId, summary, createdAt]
properties:
  id: { type: string }
  projectId: { type: string }
  summary:
    $ref: CalculationSummary.yaml
  createdAt: { type: string, format: date-time }
