/**
 * Назначение: публичный API домена catalog.
 * Описание: barrel-модуль runtime — экспортирует loadCatalog; валидация и пути к JSON остаются internal.
 */
export { loadCatalog } from './loadCatalog.js';
