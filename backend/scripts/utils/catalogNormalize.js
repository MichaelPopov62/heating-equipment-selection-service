/**
 * Назначение: нормализация каталога для seed и offline-инструментов.
 * Описание: Реэкспортирует validateAndNormalizeCatalog; не является runtime public API каталога.
 */
export { validateAndNormalizeCatalog } from '../../src/catalog/validateCatalog.js';
